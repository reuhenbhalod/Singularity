{
  "type": "object",
  "properties": {
    "steps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "action": {
            "oneOf": [
              {
                "type": "object",
                "properties": {
                  "kind": { "const": "open_url" },
                  "url": { "type": "string" }
                },
                "required": ["kind", "url"]
              },
              {
                "type": "object",
                "properties": {
                  "kind": { "const": "web_navigate" },
                  "url": { "type": "string" }
                },
                "required": ["kind", "url"]
              },
              {
                "type": "object",
                "properties": {
                  "kind": { "const": "run_script" },
                  "adapter": { "type": "string" },
                  "hook": { "type": "string" }
                },
                "required": ["kind", "adapter", "hook"]
              }
            ]
          }
        },
        "required": ["action"]
      }
    }
  },
  "required": ["steps"]
}
